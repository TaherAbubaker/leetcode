/**
 * @param {string} s
 * @return {number}
 */
var lengthOfLastWord = function(s) {
    s = s.split(" ");
    return s[s.length-1].length;
};

let s = "   fly me   to   the moon  ";

w
console.log(s);
