/**
 * @param {string} haystack
 * @param {string} needle
 * @return {number}
 */
var strStr = function(haystack, needle) {
    let index = haystack.indexOf(needle);    
    console.log(index); //if it gives a 0 then its in contains it if it gave us a -1 the dang it gang we lost
};

strStr("sadbutsad" , "sad");